spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        ip: ${nacos.host:127.0.0.1}
        namespace: public
      config:
        server-addr: localhost:8848
        namespace: public
        file-extension: yaml
        prefix: gateway
